// Get all elements with the specified class
const elements = document.querySelectorAll('.images1');

// Iterate through each element and add a click event listener
elements.forEach(element => {
  element.addEventListener('click', () => {
    // Redirect the user to the specified link when the class is clicked
    window.location.href = 'https://rajar12345.github.io/Sem3_Proj_Vmart/';
  });
});
const elements1 = document.querySelectorAll('.images2');

// Iterate through each element and add a click event listener
elements1.forEach(element => {
  element.addEventListener('click', () => {
    // Redirect the user to the specified link when the class is clicked
    window.location.href = 'https://rajar12345.github.io/dice_2/';
  });
});
const elements2 = document.querySelectorAll('.images3');

// Iterate through each element and add a click event listener
elements2.forEach(element => {
  element.addEventListener('click', () => {
    // Redirect the user to the specified link when the class is clicked
    window.location.href = 'https://rajar12345.github.io/drum_1/';
  });
});
const elements3 = document.querySelectorAll('.images4');

// Iterate through each element and add a click event listener
elements3.forEach(element => {
  element.addEventListener('click', () => {
    // Redirect the user to the specified link when the class is clicked
    window.location.href = 'https://rajar12345.github.io/stone_paper_scissor1/';
  });
});
const elements4 = document.querySelectorAll('.images5');

// Iterate through each element and add a click event listener
elements4.forEach(element => {
  element.addEventListener('click', () => {
    // Redirect the user to the specified link when the class is clicked
    window.location.href = 'https://rajar12345.github.io/rotate_img1/';
  });
});